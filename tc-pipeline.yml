groups:
- name: main
  jobs:
  - unit-tests
  - builds

resources:
- name: code-base
  type: git
  source:
    uri: https://github.com/neutrinog/translationCore
    branch: develop
- name: concourse-tasks
  type: git
  source:
    uri: https://github.com/neutrinog/concourse-tasks
    branch: master

jobs:
- name: unit-tests
  plan:
  - get: concourse-tasks
  - get: code-base
    trigger: true
    passed: [builds]
  - task: run-unit-tests
    file: concourse-tasks/tasks/unit-tests.yml
- name: builds
  plan:
  - get: code-base
  - get: concourse-tasks
  - task: build-linux
    file: concourse-tasks/tasks/build.yml
