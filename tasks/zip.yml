---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: neutrinog/node-docker
    tag: "latest"

inputs:
  - name: code-base
outputs:
  - name: zipped
run:
  path: zip
  args: [-r release/dist.zip, code-base/dist]
