
---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: neutrinog/node-docker
    tag: "8.10"

inputs:
- name: concourse-tasks
- name: code-base

outputs:
- name: build
- name: build_meta

params:
  PLATFORM: linux

run:
  path: concourse-tasks/scripts/run-build.sh
