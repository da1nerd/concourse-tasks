
---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: neutrinog/node-docker
    tag: "8.10"

inputs:
- name: concourse-tasks
- name: release

run:
  path: concourse-tasks/scripts/dropbox_uploader.sh
  args: [-t, ((dropbox-token)), release, tc-builds]
